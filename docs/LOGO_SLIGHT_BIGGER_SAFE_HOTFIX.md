# Logo Slight Bigger Safe Hotfix

This hotfix makes the website header logo a little larger than the previous safe-fit version while keeping it inside the circular frame.

## Change
- Increased image scale from `scale-[1.14]` to `scale-[1.22]`.
- Reduced padding slightly to make the logo more visible.
- Keeps the image inside the circle without intentionally cropping it.
- Does not modify the logo image file or any other navbar element.

## File
- `public_site/src/shared/design-system/components/AppLogo.tsx`
