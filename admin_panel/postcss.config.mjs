/**
 * =====================================================
 * إعداد PostCSS
 * يستخدم Tailwind CSS و Autoprefixer
 * =====================================================
 */

const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};

export default config;
